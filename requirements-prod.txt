# 🚀 프로덕션 환경용 의존성 패키지

# 웹 서버 및 API (필수)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# 컴퓨터 비전 및 이미지 처리 (필수)
opencv-python>=4.8.0

# 오디오 처리 (필수)
pyaudio>=0.2.11

# 하드웨어 제어 (Raspberry Pi, 필수)
RPi.GPIO>=0.7.1

# 비동기 프로그래밍 (필수)
asyncio-mqtt>=0.16.0

# 유틸리티 (필수)
python-multipart>=0.0.6

# 로깅 및 모니터링 (필수)
colorlog>=6.7.0

# 환경 변수 관리 (필수)
python-dotenv>=1.0.0

# 데이터 처리 (필수)
numpy>=1.24.0
Pillow>=10.0.0

# 네트워크 통신 (필수)
websockets>=11.0.0
aiohttp>=3.8.0

# 시스템 모니터링 (필수)
psutil>=5.9.0

# 날짜/시간 처리 (필수)
python-dateutil>=2.8.0

# JSON 처리 (필수)
ujson>=5.8.0

# 보안 (필수)
cryptography>=41.0.0

# 설정 관리 (필수)
pyyaml>=6.0.0

# 하드웨어 특화 라이브러리 (필수)
smbus2>=0.4.0  # I2C 통신
spidev>=3.6  # SPI 통신

# 추가 유틸리티 (필수)
requests>=2.31.0
urllib3>=2.0.0

# 머신러닝 (YOLO용, 선택사항)
# torch>=2.0.0
# torchvision>=0.15.0
# ultralytics>=8.0.0

# 데이터베이스 (선택사항)
# sqlalchemy>=2.0.0 